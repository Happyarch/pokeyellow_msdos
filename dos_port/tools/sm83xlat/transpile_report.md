# sm83xlat — the one shot (Stages 3-7)

Generated by `transpile.py`. Every figure is a measurement over pret's
`scripts/*.asm` at the SHA in README.md; re-run the tool rather than
quoting this file.

- regions lowered: **2055 / 2530 (81.2%)**
- output files: **224** (251 pret files merge into 224 port files — 26 maps are split across two or three pret sources)
- files not overwritten because the port already had one: 1

## What a bail means

A bailed region emits its verbatim pret source as a comment and **no**
symbol. Anything referencing it fails to LINK, naming the exact missing
routine. That is the entire safety argument: the failure mode is a loud
missing symbol, never a plausible wrong behaviour that assembles, runs,
and reads the wrong byte.

## Reason codes

| reason | regions | what it means |
|---|---:|---|
| `owned-by-generated-assets` | 255 |  |
| `target-region-bailed` | 107 | a CASCADE: this region jumps to a local label in a region that bailed. A NASM local label cannot be `extern`, so there is no honest lowering. Clearing an upstream bail clears these too |
| `host-pointer-in-16bit-reg` | 29 | pret puts a pointer in DE (16-bit DX here); a port HOST address is 32 bits and the callee has no abi.json entry |
| `event-byte-assembly-state` | 15 | a *Reuse* event macro whose expansion depends on the assembly-time event_byte DEF carried from a line above |
| `pointer-domain-unknown` | 15 | HL is dereferenced where the GB/HOST domain is not proven on every path |
| `inline-text-db` | 12 | a quoted glyph run — Tier-1 data, belongs to a generator |
| `local-label-scope-collision` | 11 | an intervening bail removed the global anchor, so two pret locals would collide in one NASM scope |
| `hl-half-register-access` | 10 | `ld l, a` — ESI's low byte is not addressable in 32-bit mode without REX |
| `screen-coord-projection` | 4 | the port's tilemap stride is context-dependent; a rule here would be a guess |
| `pikachu-table-index` | 4 | needs (X_id - Table) / N across object files |
| `add-hl-r16` | 4 | SM83 `add hl,r16` leaves Z alone and wraps at 16 bits; ESI is 32 |
| `checkevent-carry-form` | 3 | the 2-arg CheckEvent returns the bit in CARRY; the port macro is 1-arg only |
| `ld-via-bc-de` | 2 | `ld a, [bc]` needs a 16-bit GB pointer in BX/DX |
| `predef-leaves-parent-bank-in-a` | 1 | pret's predef leaves the parent ROM bank in A and a direct call does not, and dataflow shows a live reader of A |
| `event-macro-reuse-a-hint` | 1 |  |
| `owned-by-gen_map_script_tables` | 1 | the trainer-header tables are already generated into assets/map_script_tables.inc; emitting them here would put the same data under two owners |
| `bank-expression` | 1 | BANK(x) needs a port-side bank constant per target |

## Files with the most bailed regions

| file | lowered | bailed |
|---|---:|---:|
| `scripts/OaksLab.asm` | 63 | 29 |
| `scripts/Route22.asm` | 29 | 14 |
| `scripts/CeladonGym.asm` | 14 | 12 |
| `scripts/SilphCo7F.asm` | 29 | 12 |
| `scripts/ViridianGym.asm` | 22 | 12 |
| `scripts/FuchsiaGym.asm` | 15 | 11 |
| `scripts/MtMoonB2F.asm` | 34 | 11 |
| `scripts/Route11.asm` | 11 | 11 |
| `scripts/Route13.asm` | 11 | 11 |
| `scripts/Route14.asm` | 11 | 11 |
| `scripts/Route17.asm` | 11 | 11 |
| `scripts/SilphCo6F.asm` | 12 | 11 |
| `scripts/Route8.asm` | 10 | 10 |
| `scripts/Route3.asm` | 9 | 9 |
| `scripts/SaffronGym.asm` | 13 | 9 |


# sm83xlat — the one shot (Stages 3-7)

Generated by `transpile.py`. Every figure is a measurement over pret's
`scripts/*.asm` at the SHA in README.md; re-run the tool rather than
quoting this file.

- regions lowered: **1832 / 2530 (72.4%)**
- output files: **224** (251 pret files merge into 224 port files — 26 maps are split across two or three pret sources)
- files not overwritten because the port already had one: 1

## What a bail means

A bailed region emits its verbatim pret source as a comment and **no**
symbol. Anything referencing it fails to LINK, naming the exact missing
routine. That is the entire safety argument: the failure mode is a loud
missing symbol, never a plausible wrong behaviour that assembles, runs,
and reads the wrong byte.

## Reason codes

| reason | regions | what it means |
|---|---:|---|
| `owned-by-generated-assets` | 255 |  |
| `target-region-bailed` | 252 | a CASCADE: this region jumps to a local label in a region that bailed. A NASM local label cannot be `extern`, so there is no honest lowering. Clearing an upstream bail clears these too |
| `text-sound-command-unported` | 49 | TX_SOUND_* has no port counterpart — gb_text.inc stops at TX_DOTS/TX_WAIT_BUTTON/TX_FAR |
| `host-pointer-in-16bit-reg` | 27 | pret puts a pointer in DE (16-bit DX here); a port HOST address is 32 bits and the callee has no abi.json entry |
| `text-script-command-unported` | 16 | TX_SCRIPT_* has no port counterpart |
| `pointer-domain-unknown` | 15 | HL is dereferenced where the GB/HOST domain is not proven on every path |
| `bit-clobbers-live-carry` | 13 | SM83 `bit` preserves C, x86 `test` clears it, and C is live — no short x86 form gives the new Z and the old C |
| `bank-expression` | 13 | BANK(x) needs a port-side bank constant per target |
| `local-label-scope-collision` | 12 | an intervening bail removed the global anchor, so two pret locals would collide in one NASM scope |
| `inline-text-db` | 11 | a quoted glyph run — Tier-1 data, belongs to a generator |
| `hl-half-register-access` | 9 | `ld l, a` — ESI's low byte is not addressable in 32-bit mode without REX |
| `event-byte-assembly-state` | 6 | a *Reuse* event macro whose expansion depends on the assembly-time event_byte DEF carried from a line above |
| `screen-coord-projection` | 4 | the port's tilemap stride is context-dependent; a rule here would be a guess |
| `add-hl-r16` | 4 | SM83 `add hl,r16` leaves Z alone and wraps at 16 bits; ESI is 32 |
| `pikachu-table-index` | 3 | needs (X_id - Table) / N across object files |
| `checkevent-carry-form` | 3 | the 2-arg CheckEvent returns the bit in CARRY; the port macro is 1-arg only |
| `ld-via-bc-de` | 2 | `ld a, [bc]` needs a 16-bit GB pointer in BX/DX |
| `predef-leaves-parent-bank-in-a` | 1 | pret's predef leaves the parent ROM bank in A and a direct call does not, and dataflow shows a live reader of A |
| `unresolved-symbol` | 1 | a name that resolves in no namespace |
| `event-macro-reuse-a-hint` | 1 |  |
| `owned-by-gen_map_script_tables` | 1 | the trainer-header tables are already generated into assets/map_script_tables.inc; emitting them here would put the same data under two owners |

## Files with the most bailed regions

| file | lowered | bailed |
|---|---:|---:|
| `scripts/OaksLab.asm` | 47 | 45 |
| `scripts/GameCorner.asm` | 25 | 21 |
| `scripts/Route22.asm` | 27 | 16 |
| `scripts/ViridianGym.asm` | 18 | 16 |
| `scripts/CeladonGym.asm` | 11 | 15 |
| `scripts/FuchsiaGym.asm` | 11 | 15 |
| `scripts/MtMoonB2F.asm` | 31 | 14 |
| `scripts/SilphCo7F.asm` | 28 | 13 |
| `scripts/Museum1F.asm` | 18 | 12 |
| `scripts/SilphCo11F.asm` | 28 | 12 |
| `scripts/VermilionGym.asm` | 11 | 12 |
| `scripts/CeruleanGym.asm` | 8 | 11 |
| `scripts/PewterGym.asm` | 8 | 11 |
| `scripts/Route11.asm` | 11 | 11 |
| `scripts/Route13.asm` | 11 | 11 |


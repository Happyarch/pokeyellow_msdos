# sm83xlat — the one shot (Stages 3-7)

Generated by `transpile.py`. Every figure is a measurement over pret's
`scripts/*.asm` at the SHA in README.md; re-run the tool rather than
quoting this file.

- regions lowered: **2126 / 2275 (93.5%)**
- regions owned by generated assets: **255** — excluded from the ratio above. These are Tier-1 data the port already generates, so the tool must not emit them; they are not work it failed to do. Listed in `tables/bail_report.json` under `owned`.
- output files: **224** (251 pret files merge into 224 port files — 26 maps are split across two or three pret sources)
- files not overwritten because the port already had one: 1

## What a bail means

A bailed region emits its verbatim pret source as a comment and **no**
symbol. Anything referencing it fails to LINK, naming the exact missing
routine. That is the entire safety argument: the failure mode is a loud
missing symbol, never a plausible wrong behaviour that assembles, runs,
and reads the wrong byte.

## Reason codes

| reason | regions | what it means |
|---|---:|---|
| `target-region-bailed` | 57 | a CASCADE: this region jumps to a local label in a region that bailed. A NASM local label cannot be `extern`, so there is no honest lowering. Clearing an upstream bail clears these too |
| `host-pointer-in-16bit-reg` | 33 | pret puts a pointer in DE (16-bit DX here); a port HOST address is 32 bits and the callee has no abi.json entry |
| `pointer-domain-unknown` | 15 | HL is dereferenced where the GB/HOST domain is not proven on every path |
| `inline-text-db` | 12 | a quoted glyph run — Tier-1 data, belongs to a generator |
| `hl-half-register-access` | 9 | `ld l, a` — ESI's low byte is not addressable in 32-bit mode without REX |
| `screen-coord-projection` | 5 | the port's tilemap stride is context-dependent; a rule here would be a guess |
| `add-hl-r16` | 5 | SM83 `add hl,r16` leaves Z alone and wraps at 16 bits; ESI is 32 |
| `pikachu-table-index` | 4 | needs (X_id - Table) / N across object files |
| `local-label-scope-collision` | 3 | an intervening bail removed the global anchor, so two pret locals would collide in one NASM scope |
| `ld-via-bc-de` | 2 | `ld a, [bc]` needs a 16-bit GB pointer in BX/DX |
| `event-macro-reuse-a-hint` | 2 |  |
| `predef-leaves-parent-bank-in-a` | 1 | pret's predef leaves the parent ROM bank in A and a direct call does not, and dataflow shows a live reader of A |
| `owned-by-gen_map_script_tables` | 1 | the trainer-header tables are already generated into assets/map_script_tables.inc; emitting them here would put the same data under two owners |

## Files with the most bailed regions

| file | lowered | bailed |
|---|---:|---:|
| `scripts/Route22.asm` | 32 | 11 |
| `scripts/SilphCo6F.asm` | 12 | 7 |
| `scripts/MtMoonB2F.asm` | 37 | 6 |
| `scripts/OaksLab.asm` | 86 | 6 |
| `scripts/BikeShop.asm` | 6 | 5 |
| `scripts/CinnabarGym.asm` | 58 | 5 |
| `scripts/Daycare.asm` | 4 | 5 |
| `scripts/FuchsiaGoodRodHouse.asm` | 2 | 5 |
| `scripts/SafariZoneGate.asm` | 29 | 5 |
| `scripts/SaffronGym.asm` | 15 | 5 |
| `scripts/CeladonMartRoof.asm` | 12 | 4 |
| `scripts/GameCorner.asm` | 42 | 4 |
| `scripts/SilphCo11F.asm` | 34 | 4 |
| `scripts/SilphCo7F.asm` | 32 | 4 |
| `scripts/VermilionDock.asm` | 5 | 4 |


; SafariZoneEast.asm — translated from pret scripts/SafariZoneEast.asm by dos_port/tools/sm83xlat.
;
; ONE-SHOT OUTPUT, NOW HAND-MAINTAINED. The transpiler ran once at the SHA
; recorded in dos_port/tools/sm83xlat/README.md and is not re-run; this file is
; ordinary Tier-2 source and editing it is the normal way it changes. There is
; deliberately no DO NOT EDIT header.
;
; Every pret label is preserved verbatim so the file stays line-for-line
; cross-referenceable against the disassembly.
;
; Regions the tool could not lower WITH CERTAINTY are reproduced below as
; commented pret source under a `; BAIL[reason]` banner, and define NO symbol —
; so a reference to one is a link error rather than a plausible wrong lowering.

bits 32

%include "gb_memmap.inc"
%include "gb_constants.inc"
%include "gb_text.inc"
%include "events.inc"
%include "assets/event_constants.inc"

global SafariZoneEastRestHouseSignText
global SafariZoneEastSignText
global SafariZoneEastTrainerTipsText
global SafariZoneEast_Script
global SafariZoneEast_TextPointers

extern EnableAutoTextBoxDrawing
extern PickUpItemText

; Code and data are emitted in pret's SOURCE ORDER, in one section.
; That is not cosmetic: a NASM local label binds to the last
; non-local label above it, so hoisting the text streams into a
; separate section rebound every `.Text` to the wrong parent.
; Far text streams for this map — pret text/safari_zone_east, generated by
; tools/generators/gen_map_text.py. Defined HERE because every one is used
; only by this script, exactly as pret keeps text/<Map>.asm beside scripts/<Map>.asm.
section .data
%include "assets/map_text/SafariZoneEast.inc"

section .text

%assign event_byte -1
%assign event_byte_a -1
SafariZoneEast_Script:
    jmp EnableAutoTextBoxDrawing

%assign event_byte -1
%assign event_byte_a -1
SafariZoneEast_TextPointers:
    dd PickUpItemText
    dd PickUpItemText
    dd PickUpItemText
    dd PickUpItemText
    dd SafariZoneEastRestHouseSignText
    dd SafariZoneEastTrainerTipsText
    dd SafariZoneEastSignText
SafariZoneEastRestHouseSignText:
    text_far _SafariZoneEastRestHouseSignText
    text_end
SafariZoneEastTrainerTipsText:
    text_far _SafariZoneEastTrainerTipsText
    text_end
SafariZoneEastSignText:
    text_far _SafariZoneEastSignText
    text_end

; special_effects.asm — move effects with special handling (fallthrough pair, one terminator). $FF-terminated, scanned linearly.
;
; pret ref: data/battle/special_effects.asm:SpecialEffects/SpecialEffectsCont
;
; Generated by tools/generators/gen_effect_categories.py into assets/special_effects.inc (one .inc per pret
; source file). This carrier sits at the mirrored path dos_port/src/data/battle/special_effects.asm; it was
; part of src/data/battle_data.asm until 2026-08-16
; (docs/current_plan_data_path_mirror.md).
;
; Embedded data goes in .data per the linker rule in docs/assembly.md (.rodata has
; no output-section rule and reads back as zero).
bits 32

global SpecialEffects
global SpecialEffectsCont

section .data

%include "assets/special_effects.inc"

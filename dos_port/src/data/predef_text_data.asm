; predef_text_data.asm — TextPredefs message command streams. Include shell for
; the generated .inc, mirroring src/data/pickup_text.asm.
;
; pret's TextPredefs table (data/text_predef_pointers.asm) has 68 entries; 51 of
; them are pure printable streams and are generated here. The other 17 are Tier-2
; code (14 `text_asm` wrappers) or script dispatch markers (3 `script_*`), and are
; listed with their per-label reason in the generator's NOT_DATA table.
;
; The generated streams live in .data (linker rule: embedded data goes in .data —
; see docs/assembly.md). assets/predef_text.inc carries its own `section .data` +
; `global` per label. Generated by tools/generators/gen_predef_text.py from the
; pret engine/ wrapper labels named by data/text_predef_pointers.asm, text_far
; flattened. DO NOT hand-edit the .inc.
;
; NOT YET LINKED. Nothing consumes these streams: PrintPredefTextID's dispatch
; (src/home/predef_text.asm) still cannot link, because DisplayTextID's
; TEXT_PREDEF branch resolves entries through a 16-bit GB-space pointer walk and
; these streams are flat program-image data. See docs/current_plan_predef_text.md
; for the measured blocker and the design options. This file is assembled
; check-only so the data stays proven to build.
;
; Build: nasm -f coff -I include/ -I . -o predef_text_data.o predef_text_data.asm

bits 32

%include "assets/predef_text.inc"

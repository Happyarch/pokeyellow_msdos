; names.asm — '@'-terminated move names, walked by GetName.
;
; pret ref: data/moves/names.asm:MoveNames
;
; Generated by tools/generators/gen_move_names.py into assets/move_names.inc (one .inc per pret
; source file). This carrier sits at the mirrored path dos_port/src/data/moves/names.asm; it was
; part of src/data/battle_data.asm until 2026-08-16
; (docs/current_plan_data_path_mirror.md).
;
; Embedded data goes in .data per the linker rule in docs/assembly.md (.rodata has
; no output-section rule and reads back as zero).
bits 32

global MoveNames

section .data

%include "assets/move_names.inc"

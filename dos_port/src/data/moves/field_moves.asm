; field_moves.asm — db move id, FieldMoveNames index, leftmost tile column. $FF-terminated;
; scanned by GetMonFieldMoves.
;
; pret ref: data/moves/field_moves.asm:FieldMoveDisplayData
;
; Generated by tools/generators/gen_field_moves.py into assets/field_moves.inc. This carrier sits at the
; mirrored path dos_port/src/data/moves/field_moves.asm; it was part of src/data/field_moves.asm until 2026-08-16
; (docs/current_plan_data_path_mirror.md).
;
; Embedded data goes in .data per the linker rule in docs/assembly.md (.rodata has
; no output-section rule and reads back as zero).
bits 32

%include "gb_constants.inc"

global FieldMoveDisplayData

section .data
align 4

%include "assets/field_moves.inc"

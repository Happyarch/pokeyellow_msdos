; field_move_names.asm — variable-length '@'-terminated names, 1-based index order;
; walked by DisplayFieldMoveMonMenu.
;
; pret ref: data/moves/field_move_names.asm:FieldMoveNames
;
; Generated by tools/generators/gen_field_moves.py into assets/field_move_names.inc. This carrier sits at the
; mirrored path dos_port/src/data/moves/field_move_names.asm; it was part of src/data/field_moves.asm until 2026-08-16
; (docs/current_plan_data_path_mirror.md).
;
; Embedded data goes in .data per the linker rule in docs/assembly.md (.rodata has
; no output-section rule and reads back as zero).
bits 32

global FieldMoveNames

section .data

%include "assets/field_move_names.inc"

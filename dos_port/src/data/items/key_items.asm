; key_items.asm — 1-bit-per-item untossable flags (LSB-first, test bit item_id - 1).
;
; pret ref: data/items/key_items.asm:KeyItemFlags
;
; Generated by tools/generators/gen_items.py into assets/key_item_flags.inc. This carrier sits at the
; mirrored path dos_port/src/data/items/key_items.asm; it was part of src/data/item_data.asm until 2026-08-16
; (docs/current_plan_data_path_mirror.md).
;
; Embedded data goes in .data per the linker rule in docs/assembly.md (.rodata has
; no output-section rule and reads back as zero).
bits 32

global KeyItemFlags

section .data

%include "assets/key_item_flags.inc"

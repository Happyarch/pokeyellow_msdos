# Translation Progress

_Generated by `dos_port/tools/gen_progress_report`. Do not hand-edit — edit the generator._

_Last generated: 2026-08-26 19:49 UTC_

_Label rows come from the `translation.db` scan of `a2439e21812c6f98ca20ee84bc12175fa7030930` at 2026-08-26T15:07:49-04:00. If that commit is not near HEAD, the label tables below are older than the tree — re-run with `--scan`. The annotation ledger is always read live from `dos_port/src`._

> **Derived from the tree, not from a status table anyone maintains by hand.**
> Label rows come from `project_state` over the scanned label DB — a scan of the sources plus GNU Make's own answer about which of them link. The annotation ledger is parsed with `lint_pret_labels`'s own parser, live from `dos_port/src`. Nothing here advances because someone filed a status, so re-running the tool is all it takes to move the numbers.
>
> **What `translated` means here:** a non-stub definition exists at the label's mirrored path. It does **not** mean linked, executed, golden-matched or visually observed — those are distinct evidence. The golden suite (`make fidelity-full`) is what speaks to behaviour; this file is an inventory.

## Summary

| status | labels | share |
| --- | --- | --- |
| translated | 6630 | 93.1% |
| stub | 9 | 0.1% |
| relocated | 126 | 1.8% |
| missing | 356 | 5.0% |
| **total pret labels** | 7121 | 100% |
| _(out of scope, excluded)_ | 7 | — |


### Out of scope — excluded from the scores above

_Declared in `dos_port/tools/port_scope_exclusions.json`, a MAINTAINER-controlled registry. These are labels this port is not going to translate because it cannot in this framework — not merely ones nobody has got to yet. They are listed here rather than hidden, so the exclusion is auditable instead of a silently smaller denominator. Being unused in pret is NOT grounds for exclusion: unused-but-portable routines are ported anyway, for completeness and bug compatibility._

**`engine/movie/intro_yellow.asm`** — `Bank3E_CopyData`, `Bank3E_FillMemory`

> SM83 ROM bankswitching helpers for bank $3E. The Game Boy intro copies and fills memory across bank boundaries via these routines; in the port's flat 32-bit protected mode, all ROM/VRAM is directly addressable and memory operations use standard flat copy/fill without bank switching. Maintainer decision 2026-08-26.

**`engine/movie/title.asm`** — `TitleScreen_PlayPikachuPCM`

> The title screen's 1-bit wave-channel timer bit-bang trigger for Pikachu's title cry. In the port, Pikachu PCM is handled via audio/pikachu_pcm.asm and the Sound Blaster DSP / PC-speaker PWM HAL (src/audio/sb_pcm.asm). Maintainer decision 2026-08-26.

**`home/pikachu_cries.asm`** — `PlayPikachuPCM`, `LoadNextSoundClipSample`, `PlaySoundClipSample`

> The Game Boy's 1-bit wave-channel bit-bang loop for Pikachu's digitized voice. PlayPikachuPCM streams samples by writing rAUD3LEVEL in a tight timing loop; LoadNextSoundClipSample and PlaySoundClipSample are its inner helpers. The port cannot host this even in principle: its APU is virtual, and the per-device shims read rAUD3LEVEL ONCE PER audio_tick (src/audio/opl_shim.asm:707, src/audio/tandy_shim.asm:492), so a literal translation would emit silence rather than sound. The data model diverges too - tools/audio/gen_pika_pcm.py has already low-pass-filtered and decimated pret's 1-bit streams to 8-bit PCM. The cry itself IS implemented, faithfully and at its correct pret path: pret moved PlayPikachuSoundClip into audio/pikachu_pcm.asm (upstream bd1b695b) and the port mirrors it there, dispatching to the Sound Blaster DSP or the PC-speaker PWM player. src/audio/sb_pcm.asm:25 already carries the DEVIATION{class=HAL} for this replacement. Maintainer decision 2026-08-23.

**`home/timer.asm`** — `Timer`

> The Game Boy's timer-interrupt vector. Timer is not a routine anything calls: its sole reference in pret is home/header.asm:57 `jp Timer` at the $0050 hardware vector, and its entire body is `reti` under pret's own comment, "timer interrupt is apparently not invoked anyway" - the ROM never enables IE bit 2, so it does not run even on hardware. The port has no GB interrupt-vector table to hang it on: its timing is a native PIT channel-0 ISR installed by pit_init (dos_port/boot/entry.asm, divisor from -D PIT_DIVISOR, see boot/timing.asm), which is the DOS host's own interrupt path and shares nothing with the GB's. There is therefore no site at which a translated `reti` could be placed and nothing that would reach it. This is exclusion in the strict sense the header requires - unportable in this framework, not merely unported - and it is NOT the disallowed 'unused in pret' rationale: the bar those routines clear (GetwMoves, OverwritewMoves) is being genuinely portable, which a hardware interrupt vector on absent hardware is not. Maintainer decision 2026-08-23.

_4927 of those labels have a provider the current build links._ Port-only labels (routines with no pret counterpart — HAL boundaries, debug harnesses) are deliberately excluded: they are not pret coverage, so counting them would inflate every number here.


### By pret tier

| tier | labels | translated | % | stub | unported | what it is |
| --- | --- | --- | --- | --- | --- | --- |
| `core` | 3355 | 3143 | 93.7% | 4 | 208 | pret `home/` + `engine/` — the mirror-rule universe, the only tier with call-graph edges |
| `script` | 3766 | 3487 | 92.6% | 5 | 148 | pret `scripts/` — per-map scripts; status only, deliberately not modelled as a call graph |

## By pret subsystem

_Sorted by how much is left. `%` is translated / total._

> **Blind spot, stated so nobody reads it as a zero:** the label scanner models pret `home/`, `engine/` and (since 2026-08-20) `scripts/`. pret `audio/`, `data/`, `gfx/` and `ram/` have no rows here at all — their absence is the scanner's scope, not evidence that they are done or empty. Their labels live in the DB's `aux_labels` provenance table, which carries names only, no status.
>
> **`scripts/` rows carry a status but NO call-graph edges.** Per-map scripts are macro-heavy (`def_script_pointers`, `dw_const`, `CheckEvent`), so they are deliberately not modelled as a call graph. A script label therefore cannot be reasoned about with `--callers` or reachability, and absent edges on one mean nothing.

| subsystem | translated | stub | relocated | missing | total | % |
| --- | --- | --- | --- | --- | --- | --- |
| scripts/ | 3487 | 5 | 126 | 148 | 3766 | 92.6% |
| engine/debug | 6 | 0 | 0 | 99 | 105 | 5.7% |
| engine/printer | 74 | 0 | 0 | 35 | 109 | 67.9% |
| engine/gfx | 81 | 0 | 0 | 31 | 112 | 72.3% |
| engine/battle | 649 | 4 | 0 | 27 | 680 | 95.4% |
| engine/movie | 246 | 0 | 0 | 8 | 254 | 96.9% |
| home/ | 503 | 0 | 0 | 7 | 510 | 98.6% |
| engine/pokemon | 104 | 0 | 0 | 1 | 105 | 99.0% |
| engine/ | 7 | 0 | 0 | 0 | 7 | 100.0% |
| engine/events | 309 | 0 | 0 | 0 | 309 | 100.0% |
| engine/items | 151 | 0 | 0 | 0 | 151 | 100.0% |
| engine/link | 35 | 0 | 0 | 0 | 35 | 100.0% |
| engine/math | 14 | 0 | 0 | 0 | 14 | 100.0% |
| engine/menus | 280 | 0 | 0 | 0 | 280 | 100.0% |
| engine/minigame | 175 | 0 | 0 | 0 | 175 | 100.0% |
| engine/overworld | 218 | 0 | 0 | 0 | 218 | 100.0% |
| engine/pikachu | 227 | 0 | 0 | 0 | 227 | 100.0% |
| engine/slots | 64 | 0 | 0 | 0 | 64 | 100.0% |

## Sanctioned divergences and known defects

_Every structured `DEVIATION` / `BUG` / `GLITCH` / `STUB` annotation in `dos_port/src`, parsed by the linter. This is the queryable "why", and it travels with the code — there is deliberately no hand-kept log._

| kind | class | count |
| --- | --- | --- |
| BUG | data-model | 50 |
| BUG | timing | 2 |
| DEVIATION | HAL | 94 |
| DEVIATION | banking | 356 |
| DEVIATION | data-model | 65 |
| DEVIATION | projection | 170 |
| DEVIATION | temporary | 16 |
| DEVIATION | timing | 9 |
| GLITCH | data-model | 11 |
| STUB | stub | 4 |
| **all** |  | 777 |

### Known bugs (52)

| site | class | behavior |
| --- | --- | --- |
| `dos_port/src/audio/engine_1.asm:1038` | data-model | ascending pitch-slide span borrows from the current high byte and can be $200 too large |
| `dos_port/src/engine/battle/animations.asm:1188` | data-model | the off-screen path writes 160 to the PREVIOUS OAM entry's attribute byte as well as hiding this entry, flipping that sprite's palette/priority/flip bits |
| `dos_port/src/engine/battle/battle_transitions.asm:204` | data-model | with no live party mon (the Oak-intro scripted Pikachu battle runs with an empty party) the fainted-scan walks past the party HP words into unrelated WRAM until a nonzero pair is found |
| `dos_port/src/engine/battle/core.asm:1801` | data-model | Psywave reroll never terminates when the byte-truncated upper bound is zero |
| `dos_port/src/engine/battle/core.asm:1932` | data-model | freeze bypasses recharge-bit clearing and leaves an extra recharge turn after thaw |
| `dos_port/src/engine/battle/core.asm:2042` | data-model | full paralysis or confusion self-hit clears charging without clearing invulnerability |
| `dos_port/src/engine/battle/core.asm:2202` | data-model | confusion self-hit is redirected to the user's own Substitute |
| `dos_port/src/engine/battle/core.asm:2802` | data-model | enemy Psywave reroll never terminates when the byte-truncated upper bound is zero |
| `dos_port/src/engine/battle/core.asm:2907` | data-model | substitute overflow damage uses stale wDamage rather than the substitute's pre-hit HP |
| `dos_port/src/engine/battle/core.asm:2944` | data-model | enemy freeze bypasses recharge-bit clearing and leaves an extra recharge turn after thaw |
| `dos_port/src/engine/battle/core.asm:3079` | data-model | enemy full paralysis or confusion self-hit clears charging without clearing invulnerability |
| `dos_port/src/engine/battle/core.asm:4789` | data-model | Focus Energy and Dire Hit quarter the critical-hit threshold instead of quadrupling it |
| `dos_port/src/engine/battle/core.asm:4933` | data-model | damage can remain zero after truncation instead of being raised to the minimum one |
| `dos_port/src/engine/battle/core.asm:5041` | data-model | an accuracy threshold of 255 still misses when BattleRandom returns 255 |
| `dos_port/src/engine/battle/core.asm:5182` | data-model | wTypeEffectiveness initializes to hexadecimal $10 rather than decimal 10 |
| `dos_port/src/engine/battle/core.asm:7517` | data-model | a wild battle on Pokemon Tower 1F-7F while the player HOLDS the Silph Scope computes its run odds from a destroyed player-speed pointer and reads bag bytes as the speed |
| `dos_port/src/engine/battle/core.asm:7775` | data-model | only the high byte of accumulated Bide damage is cleared at compatibility level 0 |
| `dos_port/src/engine/battle/core.asm:7835` | timing | the wild victory jingle is started before the player mon's HP is checked at compatibility level 0, so a simultaneous faint plays the win music for a battle the player did not win |
| `dos_port/src/engine/battle/core.asm:8462` | data-model | Counter doubles stale wDamage without proving the immediately previous hit was counterable |
| `dos_port/src/engine/battle/decrement_pp.asm:38` | data-model | Struggle is not excluded from PP decrement and can underflow the selected move slot |
| `dos_port/src/engine/battle/effects.asm:213` | data-model | a target owing Hyper Beam recharge is put to sleep without status or accuracy checks |
| `dos_port/src/engine/battle/effects.asm:309` | data-model | the poison handler inherits the 1-in-256 miss for nominally perfect accuracy |
| `dos_port/src/engine/battle/effects.asm:541` | data-model | freezing the player side does not clear Hyper Beam recharge while freezing the enemy side does |
| `dos_port/src/engine/battle/effects.asm:759` | data-model | each stat-stage change reapplies badge boosts to already-boosted current stats |
| `dos_port/src/engine/battle/effects.asm:1872` | data-model | player Disable can select an enemy move slot already at zero PP outside link battles |
| `dos_port/src/engine/battle/experience.asm:510` | data-model | a multi-level EXP gain offers only the final level's move and skips intermediate learnset entries |
| `dos_port/src/engine/battle/experience.asm:586` | data-model | each additional Exp. All holder increases the divisor and dilutes every recipient's share |
| `dos_port/src/engine/battle/trainer_ai.asm:125` | data-model | the partial-iteration undo loop can write one byte before wBuffer |
| `dos_port/src/engine/battle/trainer_ai.asm:202` | data-model | write-before-decrement reaches wBuffer minus one when the first slot is minimal |
| `dos_port/src/engine/battle/trainer_ai.asm:259` | data-model | no player status returns from the whole modifier instead of only skipping status-move discouragement |
| `dos_port/src/engine/battle/trainer_ai.asm:635` | data-model | the intended 25 percent bail-out never occurs because ret nc is absent |
| `dos_port/src/engine/battle/move_effects/drain_hp.asm:67` | data-model | draining moves heal from wDamage even when a Substitute absorbed the hit |
| `dos_port/src/engine/battle/move_effects/heal.asm:127` | data-model | recovery fails when the HP deficit is congruent to 255 modulo 256 because the high-byte comparison is discarded |
| `dos_port/src/engine/battle/move_effects/heal.asm:175` | data-model | Rest clears poison status but leaves BADLY_POISONED and the Toxic counter stale |
| `dos_port/src/engine/battle/move_effects/substitute.asm:108` | data-model | current HP exactly equal to one quarter max HP permits Substitute and leaves the user at zero HP |
| `dos_port/src/engine/battle/move_effects/transform.asm:78` | data-model | enemy-turn invulnerability testing reads hWhoseTurn after A was clobbered instead of a battle-status byte |
| `dos_port/src/engine/battle/move_effects/transform.asm:115` | data-model | player-turn Transform checks the user's status rather than the target's invulnerability |
| `dos_port/src/engine/events/hidden_events/vermilion_gym_trash2.asm:61` | data-model | case 3 writes the sampled offset to B instead of A so the value returned in E is the raw swapped random byte 0-255 instead of 0-2, which makes Yellow_SampleSecondTrashCan read up to 510 bytes past the row |
| `dos_port/src/engine/gfx/bg_map_attributes.asm:450` | data-model | which badge faces get their attribute zeroed is decided by stale unrelated data rather than by which badges the player owns so the outcome depends on what ran before the trainer card was opened |
| `dos_port/src/engine/items/item_effects.asm:2078` | data-model | a captured transformed wild mon is restored as Ditto regardless of its original species |
| `dos_port/src/engine/items/item_effects.asm:2132` | data-model | dex-number zero wraps to bit 255 and writes beyond wPokedexOwned after capture |
| `dos_port/src/engine/items/item_effects.asm:2641` | data-model | Max Ether and Max Elixer do not mask the PP-Up count bits before comparing current PP against max PP, so a move that has had any PP Up used on it is never detected as already at full PP and the item is consumed for no effect |
| `dos_port/src/engine/items/item_effects.asm:3679` | data-model | reads the first opcode of GetTileAndCoordsInFrontOfPlayer instead of wTileInFrontOfPlayer so every door comparison fails |
| `dos_port/src/engine/menus/options.asm:113` | timing | held direction can edit the opening option because joypad state is not cleared before initialization |
| `dos_port/src/engine/overworld/movement.asm:1307` | data-model | the $11-$14 movement path decrements H from data1 into audio WRAM and writes value 3 |
| `dos_port/src/engine/pokemon/add_mon.asm:670` | data-model | withdrawing a low-level Medium-Slow mon can inherit CalcExperience underflow as level 100 and softlock |
| `dos_port/src/engine/pokemon/learn_move.asm:166` | data-model | level-up move refresh overwrites Mimic's temporary battle move with the party move array |
| `dos_port/src/engine/slots/slot_machine.asm:759` | data-model | in seven-and-bar mode the wheel was likely intended to stop when a 7 symbol is visible but cp HIGH(SLOTS7) followed by jr c never branches because all symbol tile IDs are at least HIGH(SLOTS7) |
| `dos_port/src/engine/slots/slot_machine.asm:987` | data-model | copies $1c tiles of SlotMachineTiles2 where the blob is $18 tiles, over-reading 4 tiles past its end on both copies |
| `dos_port/src/home/names2.asm:34` | data-model | HM01 threshold redirects every name type, not only items, to machine-name formatting |
| `dos_port/src/scripts/CeruleanTrashedHouse.asm:86` | data-model | pret ANDs the bag quantity with the caller ROM bank left in A by predef so the TM reads as absent for any quantity that is a nonzero multiple of 8, and the port tests quantity against zero instead |
| `dos_port/src/scripts/Daycare.asm:332` | data-model | pret loads Bankswitch into HL instead of calling it so SchedulePikachuSpawnForAfterText is never invoked and the Pikachu spawn is not scheduled after the text, and the port omits the three dead loads entirely |

### Preserved glitches (11)

| site | class | behavior |
| --- | --- | --- |
| `dos_port/src/engine/battle/core.asm:6462` | data-model | the Leech Seed call increments Toxic's counter a second time in the same turn |
| `dos_port/src/engine/battle/core.asm:6466` | data-model | BX can exceed actual HP taken and over-heal the seeder |
| `dos_port/src/engine/battle/core.asm:6542` | data-model | Toxic multiplication executes for Leech Seed as well as poison and burn |
| `dos_port/src/engine/battle/core.asm:8362` | data-model | Jump Kick and Hi Jump Kick crash recoil is always one HP because wDamage is zero before the shifts |
| `dos_port/src/engine/battle/effects.asm:1799` | data-model | the human chooses a copied move while enemy AI and link players receive a random move |
| `dos_port/src/engine/items/inventory.asm:39` | data-model | corrupt count or missing terminator can drive inventory scans beyond the fixed list |
| `dos_port/src/engine/items/item_effects.asm:1813` | data-model | the old-man tutorial copies the player name over encounter data at wGrassRate |
| `dos_port/src/engine/menus/swap_items.asm:150` | data-model | 8-bit quantity merge can wrap an exact sum of 256 to a zero-count stack and open the item-underflow chain |
| `dos_port/src/engine/pikachu/pikachu_pic_animation.asm:941` | data-model | the table-full and already-loaded exits return without unwinding the two saved registers, so the ret consumes the saved HL as its return address and transfers control to it - reproduced literally here, where the saved ESI is a small GB offset and the transfer faults instead of executing WRAM |
| `dos_port/src/engine/pokemon/experience.asm:129` | data-model | level-1 Medium-Slow subtraction underflows the 24-bit accumulator and can report level 100 |
| `dos_port/src/home/names2.asm:39` | data-model | out-of-range name indices walk beyond the source table until a terminator and produce glitch names |

### Annotated stubs (4)

_Link-time stand-ins carrying a `STUB{...}` annotation. This count is NOT the `stub` row in the summary above and is not expected to match it: that row counts pret labels whose provider is a `*_stubs.asm` body, while this counts annotation SITES, and one site can cover several labels. The two are already reconciled where it matters — `lint_pret_labels` checks every STUB annotation's `label` field against the scanned status and reports a violation if they disagree._

| label | site | lifetime |
| --- | --- | --- |
| `SetupPlayerAndEnemyPokeballs` | `dos_port/src/engine/battle/battle_stubs.asm:59` | until a widescreen placement for the link-battle versus pokeball rows is decided (maintainer/plan call, not an agent guess) |
| `TradeHidePokemon` | `dos_port/src/engine/battle/core_stubs.asm:159` | until the Phase 4 trade animation engine lands |
| `TradeShakePokeball` | `dos_port/src/engine/battle/core_stubs.asm:164` | until the Phase 4 trade animation engine lands |
| `TradeJumpPokeball` | `dos_port/src/engine/battle/core_stubs.asm:169` | until the Phase 4 trade animation engine lands |


---

_To reproduce: `dos_port/tools/gen_progress_report`. For a single label's provider, linkage and callers, use `dos_port/tools/project_state <Label>`._

